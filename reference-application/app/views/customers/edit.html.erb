<% provide(:title, 'Edit customer') %>
<h1>Editing Customer</h1>

<%= form_for(@customer, url: customer_path(@customer), as: :customer) do |f| %>
  <%= render 'form', customer: @customer, f: f %>
<% end %>
<%= link_to 'Back', customers_path, class: 'btn btn-default' %>
